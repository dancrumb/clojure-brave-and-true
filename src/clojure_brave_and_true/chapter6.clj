(ns clojure-brave-and-true.chapter6 (:gen-class))

(use 'clojure.pprint)

; actually nothing in here, since this chapter is just about namespaces with no exercises

(defn -main [& args] (clojure.pprint/pprint '(nil)))
